import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    subtitle: {
        fontSize: 14,
        fontWeight: 'bold',
        marginTop: 5,
        marginBottom: 8,
        color: '#2c3e50',
        textAlign: 'center',
    },

});
export default styles;